{
  "name": "cloud-run-batch-experiment",
  "version": "1.0.0",
  "private": true,
  "description": "Deploy and run batch Python experiments on GCP Cloud Run Jobs with parallel execution",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "type": "module"
}
